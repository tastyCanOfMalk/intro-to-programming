namespace Banking.Domain;

public class BusinessHoursBonusCalculator : ICalculateBonuses
{
    public decimal GetBonusForDepositOn(decimal currentBalance, decimal amountOfDeposit)
    {
        // all your new work goes here.
        return -42;
    }
}